using UnityEngine;
using System.Collections;

public class Storage : MonoBehaviour {

	/*

	void OnTriggerEnter(Collider other)
	{
		if (isIt != null) {
			StoreAll (isIt);
		}
	}


	public void StoreAll (NPC target)
	{
			DBRessources.AddRessourceToDB(RessourceType.WOOD, target.GetWoodCargo());
			target.AddCargo (-target.GetWoodCargo(), RessourceType.WOOD);

			DBRessources.Add(RessourceType.STONE, target.GetStoneCargo());
			ziel.AddCargo (-target.GetStoneCargo(), RessourceType.STONE);
		
			DBRessources.Add(RessourceType.FOOD, target.GetFoodCargo());
			target.AddTCargo (-target.GetFoodCargo(), RessourceType.FOOD);
	}

*/
}
